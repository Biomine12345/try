/**
 * Footer initializations that should be applied to all pages.
 */
(function($, Footer, undefined) {
  "use strict";

  // Initializes the module.
  Footer.init = function() {
  };

}(jQuery, window.Footer = window.Footer || {}));
